/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['i.ibb.co', 'assets.adidas.com', 'cdn.shopify.com'],
  },
}

module.exports = nextConfig
